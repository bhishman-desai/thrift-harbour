stages:
  - triggers

frontend-trigger:
  stage: triggers
  trigger:
    include: frontend/.gitlab-ci.yml

backend-trigger:
  stage: triggers
  trigger:
    include: backend/.gitlab-ci.yml
  rules:
    - changes:
        - backend/*
